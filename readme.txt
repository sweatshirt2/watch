
I used the context api to sign-up and log-in users.

The usercontext is a context for signing-up users, while the usercontext manages signed-up and pre-defined users.

Logging in unlocks new features such as ellapsing more than three times and getting a header to post a motto.

happy coding ...
